[project]
name = "baron-tts-service"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.111.0",
  "uvicorn[standard]>=0.29.0",
  "neutts[all]",
  "torch>=2.0.0",
  "soundfile>=0.12.1",
  "requests>=2.31.0",
  "pyarrow>=16.0.0",
  "datasets>=2.20.0",
]

[tool.uv]
package = false
index-url = "https://pypi.org/simple"
